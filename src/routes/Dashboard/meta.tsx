import { RouteMeta } from "../types";

const routeMeta: RouteMeta = {
  titleDefault: "Dashboard",
  slug: "dashboard",
  path: "/dashboard",
  permissions: [],
  hidden: false,
};
export default routeMeta;
